# Python bytecode
__pycache__/
*.pyc
*.pyo

# Secrets — never commit API keys
.env
.env.local
.env.*.local
*.key
*_api_key*

# Local context files — not for git
CLAUDE.md
STATUS.md

# Live Gemini model-list cache (regenerated on demand)
.gemini_models.json
